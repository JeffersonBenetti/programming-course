// - Operadores Aritméticos:

// - Os operadores aritméticos no JavaScript são usados para realizar operações matemáticas em valores numéricos. Existem vários operadores aritméticos disponíveis, incluindo adição, subtração, multiplicação, divisão, resto da divisão, incremento e decremento.

// - Adição (+): O operador de adição é usado para somar dois valores.

// - Exemplo:
    let adi = 5;
    let bAdi = 3;
    let resultAdi = a + b;
    console.log(resultAdi); // Saída: 8

// - Subtração (-): O operador de subtração é usado para subtrair um valor de outro.

// - Exemplo:
    let Sub = 5;
    let SubT = 3;
    let resultSub = a - b;
    console.log(resultSub); // Saída: 2

// - Multiplicação (*): O operador de multiplicação é usado para multiplicar dois valores.

// - Exemplo:
    let mult = 5;
    let multP = 3;
    let resultMult = a * b;
    console.log(resultMult); // Saída: 15

// - Divisão (/): O operador de divisão é usado para dividir um valor pelo outro.

// - Exemplo:
    let div = 10;
    let divisao = 2;
    let resultDiv = a / b;
    console.log(resultDiv); // Saída: 5

// - Resto da Divisão (%): O operador de resto da divisão retorna o resto de uma divisão.

// - Exemplo:
    let divRest = 10;
    let divResto = 3;
    let resulRest = a % b;
    console.log(resultRest); // Saída: 1


// - Incremento (++): O operador de incremento é usado para aumentar o valor de uma variável em 1.

// - Exemplo:
    let incr = 5;
    a++;
    console.log(PictureInPictureWindow); // Saída: 6

// - Decremento (--): O operador de decremento é usado para diminuir o valor de uma variável em 1.

// - Exemplo:
    let decr = 5;
    a--;
    console.log(decr); // Saída: 4

// - Esses são os operadores aritméticos básicos no JavaScript. Eles podem ser usados em expressões matemáticas mais complexas e combinados para realizar cálculos mais avançados. Lembre-se das regras de precedência dos operadores matemáticos para garantir a ordem correta das operações.